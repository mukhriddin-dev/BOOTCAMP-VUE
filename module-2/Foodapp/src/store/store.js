import { createStore } from "vuex";
import increment from "../service/increment";
export const store = createStore({
  state() {},
  mutations: {},
  actions: {},
  getters: {},
  modules: {increment},
});
